import { StyleSheet } from "react-native";

export default styleLayout = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 40,
    position: "relative",
  },
});
